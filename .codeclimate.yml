version: "2"
checks:
  method-complexity:
    config:
      threshold: 10
engines:
  eslint:
    enabled: true
  duplication:
    enabled: true
    checks:
      Similar code:
        enabled: false
ratings:
  paths:
  - "**.**"
exclude_paths:
- "**/test/**/*_test.js"
